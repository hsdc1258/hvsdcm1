{
  "name": "hvsdcm-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "db:init": "wrangler d1 migrations apply hvsdcm --local",
    "test": "node --test test.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.92.0"
  }
}
