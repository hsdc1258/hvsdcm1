# plan.md — 사이클3: 사회문화 개념 파트 수능형 리디자인

- 작성: Fable 5 (오케스트레이터, 본 세션)
- 대상: `smstudy/` (사회·문화 학습 화면), 정적 사이트 / 빌드 없음
- 날짜: 2026-08-26

---

## 0. 현황 계측 (조사 단계 결과, Explore 서브에이전트)

리디자인의 근거가 되는 실측값이다. 이후 완료 조건은 전부 이 수치를 기준으로 잡는다.

| 항목 | 실측 |
|---|---|
| 개념 데이터 | `smstudy/assets/js/notebook-data.js` — 13개 중단원, 문자열 리터럴 821개 |
| `oneLine` (단원 헤드라인, h3) | 13개 / 평균 **90자** / 최장 105자 |
| `examInsight` | 13개 / 평균 **86자** / 최장 97자 |
| `deepDive[].body` | 62개 / 평균 68자 / 최장 **139자** |
| 60자 초과 문자열 | 83개 (80자 초과 28개) |
| 한 문자열에 2문장 이상 | **93개** |
| 줄바꿈 | 리터럴 개행 0개, br 태그 0개, 마크다운 0개 — **전부 개행 없는 평문 한 줄** |
| 렌더 | `app.js:630 renderConcept()` — 데이터 문자열은 예외 없이 `escapeHtml()` 통과 |
| 토큰 | `assets/css/system.css`가 단일 원본(73개). `smstudy/assets/css/style.css`에 `:root` 없음 |
| 테마 | 다크 전용. 라이트 팔레트는 `@media print` 경로에만 존재 |
| 기출 | `data.js` `QUESTION_ROWS` 78문항 + `assets/kice/*.webp` 78장 |

**진단**: 사용자가 말한 "문장이 길고 줄바꿈이 뒤죽박죽"의 실제 원인은 *문장 길이*가 아니라
**스키마에 구조가 없다는 것**이다. `oneLine`·`examInsight`·`deepDive.body`가 단일 string이라
렌더러가 분절할 방법이 없고, `esc()` 정책 때문에 데이터에 br 태그를 넣는 우회도 막혀 있다.
따라서 CSS만 손봐서는 해결되지 않으며 **데이터 스키마를 구조화하는 것이 정공법**이다.

---

## 1. 요구사항 (검증 가능한 문장으로)

- R1. 개념 파트의 모든 본문 문자열은 **한 문장, 60자 이하**다. 헤드라인 역할 필드는 **30자 이하**다.
- R2. 시각적 분절은 CSS가 아니라 **데이터 구조**(배열·객체)가 만든다. 데이터에 개행·태그를 넣지 않는다.
- R3. 각 중단원은 **다이어그램을 최소 1개** 가진다. 형식은 고정하지 않는다 —
  **그 단원의 내용 구조에 맞는 형식을 구현자가 고른다.** 사용자가 첨부한 방사형 마인드맵은
  가능한 형식의 *예시일 뿐 사양이 아니다*. 선택지: 방사형 마인드맵 / 피라미드(위계·포함관계) /
  플로우·의사결정 트리(판별 순서) / 2×2 매트릭스(교차 분류) / 비교 저울(대립 개념) /
  타임라인·단계도(과정) / 벤 다이어그램(중첩) / 막대·게이지(빈출·정답률).
  **한 단원에 형식 1개만 쓰라는 제약도 없다.** 어떤 형식을 왜 골랐는지 단원별로 한 줄씩 남긴다.
- R4. 아이콘을 적극적으로 쓴다. **외부 아이콘 세트를 써도 된다** — 다만 CDN 런타임 의존은
  만들지 않고 `assets/vendor/`에 **벤더링**한다(정적 배포·오프라인 열람·CSP 안정성).
  데이터는 아이콘 **키 문자열만** 갖고 마크업은 렌더러가 만든다(`esc()` 정책 유지).
- R5. 개념과 기출이 **기계적으로 연결**된다. `QUESTION_ROWS`에 개념 태그 필드를 신설하고,
  개념 화면의 빈출도·정답률·난이도는 **수기 숫자가 아니라 78문항에서 자동 집계**한 값이다.
- R6. 콘텐츠는 **수능 문제풀이용**으로 재작성한다. 최근 5개년 평가원 기출에 실제로 나온 개념·함정만
  남기고, 출제되지 않는 지엽적 서술은 삭제한다. 삭제 판단 근거를 §7 기준에 따라 남긴다.
- R7. 기존 자동 게이트(`npm test`)를 통과한다. 단, **콘텐츠 문자열 하드코딩 검사 7건은 구조 계약
  검사로 대체**한다(§6 LESSONS 규칙 5). 기준선 `2c49cb5`의 실측은 `NOTEBOOKS` 5건 +
  `explanation-data` 2건 = **7건**이다(리뷰 M-7: 이 문서의 "6건"과 kice-analysis 부록 E의
  "5건"이 둘 다 틀렸다. 부록 E는 `NOTEBOOKS` 쪽만 센 값이었다).
- R8. 새 색 토큰을 도입하지 않는다. 불가피하면 `system.css`에 추가하고 print 팔레트와
  대비표 주석을 함께 갱신한다(CLAUDE.md 승격 규칙 "토큰 대비 전수 검산").

---

## 2. 기출 분석 범위 — 사용자 요청과의 차이 (판단 기록)

사용자 요청은 "2023–2027 수능"이었다. 실제 가능한 범위는 다르며, 다음과 같이 확정한다.

- 레포의 기출 자산은 **2022~2026학년도**다(파일명 `{학년도}-{csat|june|september}-{번호}.webp`).
- **2027학년도 수능은 2026년 11월 시행으로 아직 치러지지 않았다.** 2027학년도 6월 모평(2026년 6월)은
  시행됐으나 레포에 자산이 없다.
- 2025학년도 수능 이미지가 누락돼 있다(6월·9월만 존재).

**결정**: 분석 범위를 **2022~2026학년도 5개년 78문항(수능 4회 + 6월 5회 + 9월 5회)**으로 확정하고,
목표를 **2027학년도 수능 대비**로 잡는다. 누락분(2025학년도 수능, 2027학년도 6월)은 자산이 없어
이번 사이클에서 다루지 않으며, `docs/kice-analysis.md`에 "미수록"으로 명시한다.
사용자가 원한 "최근 기출의 출제 방향 파악"이라는 목적은 78문항으로 충족된다고 판단해 진행한다.

---

## 3. 파일 구조 / 변경 대상

| 파일 | 책임 | 변경 성격 |
|---|---|---|
| `docs/kice-analysis.md` | **신규**. 78문항 판독 결과 — 문항별 개념 태그, 단원별 빈출 개념·출제 스타일·함정 유형 | 생성 |
| `smstudy/assets/js/notebook-data.js` | 개념 데이터. 스키마 구조화 + 전 단원 콘텐츠 재작성 | 전면 개편 |
| `smstudy/assets/js/data.js` | `QUESTION_ROWS[].tags` 신설(78건). `UNITS[].sections`·`visual` 문장 축약 | 필드 추가 + 문장 수정 |
| `smstudy/assets/js/app.js` | `renderConcept()` 계열 렌더러 — 마인드맵 SVG 조립, 아이콘 심볼 맵, 기출 집계 | 렌더 함수 개편 |
| `smstudy/assets/css/style.css` | 마인드맵·아이콘·인포그래픽 칩/게이지 스타일. 기존 토큰만 사용 | 클래스 추가 |
| `scripts/validate.mjs` | 콘텐츠 하드코딩 6건 → 구조 계약 검사로 교체, 신규 계약 추가 | 게이트 교체 |
| `smstudy/index.html` | 변경 없음 예정 (새 JS 파일을 만들지 않는다 — 스크립트 순서 계약 보호) | 무변경 |

**새 파일 추가는 허용한다.** 다이어그램 렌더러가 커지면 `smstudy/assets/js/diagram.js`처럼
분리해도 되고, 아이콘 세트를 `assets/vendor/`에 벤더링해도 된다. 단 파일을 추가하면
`validate.mjs`의 `firstPartyCss` 등록부와 `validateGlobalsAndOrder()`의 스크립트 순서 목록,
`index.html`의 로드 순서를 **같은 커밋에서 함께 갱신**한다 — 이 셋이 곧 배포 계약이다
(CLAUDE.md: "파일명·스크립트 로드 순서가 곧 배포 계약"). 갱신 없이 파일만 늘리면 게이트가 막는다.

---

## 4. 인터페이스 계약

### 4.1 `notebook-data.js` — `NOTEBOOKS[subId]` 새 스키마

기존 9개 필드 중 단일 string이던 3개를 구조화하고, 마인드맵을 신설한다.

```
{
  headline: string,            // (신) oneLine 대체. 명사구, <=30자, 종결부호 없음
  summary: string[],           // (신) 2~3개, 각 <=60자 1문장

  diagrams: [                  // (신) R3. 1~2개. 형식은 단원 내용에 맞게 고른다
    { kind: 'radial'|'pyramid'|'flow'|'matrix2x2'|'scale'|'timeline'|'venn',
      title: string,           // <=20자
      why: string,             // <=40자 — 이 형식을 고른 이유 (사람용 주석 겸 문서)
      center?: string,         // radial 전용, <=14자
      nodes: [                 // kind별 해석은 4.3 렌더러 계약 참조
        { label: string,       // <=12자
          icon?: string,       // 심볼 키 (4.3)
          items?: string[] }   // 0~4개, 각 <=20자
      ]
    }
  ],

  keyPoints: [{ label: string, // <=12자
                icon: string,
                text: string }],// <=60자 1문장. 정확히 3개 (기존 계약 유지)

  exam: {                      // (신) examInsight 대체
    trend: string,             // <=60자 1문장 — 어떻게 출제되는가
    trap: string,              // <=60자 1문장 — 무엇으로 틀리게 만드는가
    tags: string[]             // 이 단원의 개념 태그 집합 (QUESTION_ROWS[].tags의 원본)
  },

  matrix: { title, headers, rows },  // 기존 계약 유지(headers>=3, rows>=4). 각 셀 <=24자
  decision: string[],          // >=4개, 각 <=60자 1문장
  deepDive: [{ term: string,   // <=20자
               icon: string,
               points: string[] }],  // (신) body 대체. 2~4개, 각 <=60자 1문장. term 총 >=4개
  recall: [{ question, answer }]      // >=3개, 각 <=60자
}
```

- **삭제**: `patterns[]`. 수기 `count`가 자동 집계로 대체되므로 존재 이유가 없다(R5, LESSONS 5).
  `patterns[].label`이 하던 역할은 `exam.tags`가 이어받는다.
- `LEARNING_DESIGN`은 형태 유지, 문장만 R1에 맞게 축약.

### 4.2 `data.js` — `QUESTION_ROWS[]` 필드 추가

```
{ id, sub, year, session, number, answerNumber, correctRate, wrongRate, image,
  tags: string[] }   // (신) 1~3개. 값은 반드시 NOTEBOOKS[sub].exam.tags 안에 존재
```

집계 규칙(렌더러가 계산, 데이터에 저장하지 않음):

- 태그 빈출도 = 해당 태그를 가진 문항 수 / 단원 총 문항 수
- 단원 난이도 = 해당 단원 문항의 `correctRate` 평균
- "고난도" 표시 = `wrongRate >= 35` (기존 `weak` 규칙 재사용)

### 4.3 `app.js` — 렌더러 계약

- `ICONS`: 키 → SVG path 문자열 맵. 외부 아이콘 세트(예: Lucide, MIT)를 벤더링해 채워도 된다.
  데이터의 `icon` 값이 맵에 없으면 렌더러는 아이콘을 생략한다(깨진 마크업 금지).
- `renderIcon(key)` → `<svg class="sm-icon" viewBox="0 0 24 24" aria-hidden="true">`.
  아이콘은 항상 장식이며 의미는 옆의 텍스트가 전달한다(스크린리더 중복 금지).
- `renderDiagram(d)` → `<figure class="sm-diagram sm-diagram--{kind}">` 안에 인라인 SVG.
  `kind`별 레이아웃 함수를 각각 구현한다. 공통 규칙:
  - `viewBox` 고정 + `width:100%`로 반응형. 텍스트는 SVG `<text>`로 넣되 라벨 길이 상한이
    스키마에 걸려 있으므로 넘침이 나지 않는다.
  - 색은 `currentColor`와 CSS 클래스로만 지정한다(SVG 속성에 색 리터럴 금지 — 토큰 규칙).
  - **좁은 화면에서는 SVG 대신 같은 데이터의 목록 폴백으로 전환한다.** `hidden` 속성이 아니라
    CSS 미디어쿼리로 전환한다(validate의 `hidden` 회귀 검사 저촉 방지).
  - 접근성: `figcaption`에 다이어그램이 말하는 바를 문장으로 서술한다.
- 애니메이션·전환을 써도 되지만 `prefers-reduced-motion: reduce`에서 반드시 끈다.
- `renderExamAnalysis(sub)` → 태그별 빈출 막대 + 정답률 게이지. 값은 `QUESTIONS`에서 계산.
- 모든 데이터 문자열은 **기존대로 `esc()`를 통과**한다. 예외를 만들지 않는다.

---

## 5. 완료 조건

기계 검사(`npm test`)로 판정한다. 각 항목 옆이 이를 검증하는 검사다.

- [ ] `npm test` 통과 (빌드 단계 없음)
- [ ] **길이 계약**: `NOTEBOOKS`의 모든 본문 문자열 <=60자, `headline` <=30자, `map.center` <=14자,
      `branch.label` <=12자, `map.items[]` <=20자 → `validate.mjs`가 스키마를 순회하며 **자동 도출**해 검사
- [ ] **1문장 계약**: 본문 문자열에 종결부호가 2회 이상 나오지 않는다 → 동일 순회 검사
- [ ] **평문 계약**: 데이터에 꺾쇠·개행·br 태그가 없다 → 동일 순회 검사
- [ ] **다이어그램 계약**: 13단원 전부 `diagrams` 1개 이상 보유, `kind`가 허용 집합에 속하고,
      `kind` 목록은 `app.js`의 레이아웃 함수 이름에서 **자동 도출**한다(하드코딩 금지).
      nodes 개수는 kind별 상·하한을 만족한다 → 형태 검사
- [ ] **형식 다양성**: 13단원이 전부 같은 `kind`를 쓰지 않는다(최소 4종 이상 사용).
      단원 내용에 맞춰 골랐다는 R3를 기계적으로 뒷받침한다
- [ ] **아이콘 계약**: 데이터의 모든 `icon` 값이 `app.js`의 `ICONS` 키 집합에 존재.
      키 목록은 `app.js`에서 **정규식으로 파싱해 자동 도출**한다(하드코딩 금지)
- [ ] **태그 정합**: 78문항의 모든 `tags` 값이 `NOTEBOOKS[sub].exam.tags`에 존재하고,
      **역방향으로 모든 `exam.tags`가 최소 1문항에 쓰인다**(죽은 태그 금지) → 양방향 자동 대조
- [ ] **하드코딩 제거**: `validate.mjs`에서 기존 콘텐츠 문자열 검사 7건이 사라지고,
      그 자리를 위 구조 계약이 대신한다
- [ ] **음성 테스트**: 위 신규 검사 각각에 대해 "일부러 값을 위반시키면 실제로 실패하는가"를
      1건씩 확인하고 결과를 `docs/review.md`에 적는다 (LESSONS 규칙 4)
- [ ] **대비**: 새 토큰을 도입했다면 배경 조합별 대비를 계산해 토큰 정의 옆 주석에 남긴다.
      도입하지 않았다면 "신규 토큰 0개"를 리뷰에 명시 (CLAUDE.md 승격 규칙)
- [ ] **수동 확인 1건**: 320px / 768px / 1280px에서 다이어그램이 잘리거나 겹치지 않는다.
      구현자가 브라우저 프리뷰 스크린샷으로 증명한다(시각 판단은 기계 검사로 대체 불가 —
      이것이 이번 게이트의 사각지대다, LESSONS 규칙 6)

---

## 6. 관련 LESSONS 규칙 (인용)

- **규칙 5** — "색 토큰·훅 이름 같은 검사 대상 목록을 하드코딩하지 않는다. 단일 원본에서 파싱해
  자동 도출하면 원본에 항목이 늘어도 검사가 뒤처지지 않는다."
  → `patterns[].count` 수기 숫자와 콘텐츠 문자열 6건을 제거하는 직접 근거다.
- **규칙 4** — "두 개의 독립된 존재 검사를 AND로 묶어 요소 하나를 검증한 것처럼 쓰지 않는다.
  값을 지우는 음성 테스트로 실제로 실패하는지 확인한다." → §5 음성 테스트 항목.
- **규칙 6** — "텍스트 검사로 커버할 수 없는 자산이 있으면 별도 잠금 검사를 둔다.
  검사 통과가 곧 요구사항 충족은 아님을 전제한다." → 다이어그램의 시각적 정합성은 grep으로
  판정할 수 없다. §5 마지막 항목에서 사각지대를 명시하고 스크린샷으로 메운다.
- **규칙 1·2 (승격됨)** — 토큰 대비 전수 검산. → R8.
- **규칙 10** — "줄바꿈/공백 정규화는 로직 변경과 별도 커밋으로 분리한다."
  → 콘텐츠 재작성(데이터)과 렌더러/CSS 변경을 별도 커밋으로 나눈다.
- **규칙 8** — "문서 산출물은 작성 도중 자주 커밋한다." → `kice-analysis.md`는 판독 배치마다 커밋.

---

## 7. 콘텐츠 삭제 기준 (R6 판단 근거)

`docs/kice-analysis.md`의 5개년 집계에서 **0회 출제된 하위 개념**은 삭제 후보로 표시하고,
다음 중 하나에 해당하면 남긴다: (a) 다른 빈출 개념을 이해하는 데 전제가 되는 정의,
(b) 오답 선지로 반복 등장하는 개념. 그 외는 삭제한다. 삭제한 항목은 `kice-analysis.md`에
"제외" 목록으로 남겨 되살릴 수 있게 한다.

---

## 8. 담당 지정

| 단계 | 모델 | 근거 |
|---|---|---|
| 2. 기획 | **Fable 5** (본 세션) | CLAUDE.md 고정 |
| 2.5 기출 판독 (신설, 격리) | **sonnet 5** × 3 병렬 | 78장 webp에서 개념 태그를 뽑는 추출 작업. 사양이 확정돼 있어 sonnet으로 충분하고, 이미지 토큰이 크므로 서브에이전트에 격리한다. 판독 결과는 구현자가 검수한다 |
| 3. 구현 | **opus 5** | **Codex 제외 판정**: 이번 작업의 본체는 마인드맵 SVG 레이아웃·아이콘·정보 위계 등 **시각적 판단**과 개념 콘텐츠 저술이다. CLAUDE.md는 "프론트엔드는 Codex에 맡기지 않는다"고 못박고 있고, 사양이 확정된 기계적 스타일 작업이 아니라 설계 판단이 필요하므로 sonnet이 아닌 opus다 |
| 4. 기계 게이트 | 본 세션 | `npm test` |
| 5. 리뷰 | **Codex** (`codex exec`) | Claude 계열이 구현했으므로 교차 검토 유지 (CLAUDE.md 모델 배정) |
| 6. 수정 | **opus 5** | "자기 코드의 지적사항은 자기가 고친다" |
| 8. 회고 | **sonnet 5** | /retro 규칙 |

- 실행 위치: 2.5·3·5·6·8 전부 서브에이전트 (CLAUDE.md 토큰 규칙 "실행 격리")
- Codex 한도: 리뷰 1회분만 사용하므로 전환 불필요. 실패 시 폴백은 opus가 아닌 **sonnet 5**로 잡는다
  (구현자가 opus이므로 opus 리뷰는 동일 모델 자기검토가 되어 교차 검토가 깨진다).

---

## 9. 커밋 계획

1. `docs(kice): 2022~2026학년도 78문항 판독 분석` — `docs/kice-analysis.md`
2. `feat(smstudy): 개념 데이터 스키마 구조화 + 5개년 기출 기반 재작성` — `notebook-data.js`, `data.js`
3. `feat(smstudy): 다이어그램·아이콘 인포그래픽 렌더러` — `app.js`(+분리 시 `diagram.js`), `style.css`, `assets/vendor/`
4. `test(validate): 콘텐츠 하드코딩 검사를 구조 계약 검사로 교체` — `scripts/validate.mjs`

---

## 10. 실행 중 판단 기록 (오케스트레이터)

- **2.5단계 결과**: 78문항 전량 판독, 판독불가 0건. `docs/kice-batch-{a,b,c}.md` 생성.
  `QUESTION_ROWS[].sub`와 실제 문항 내용이 불일치하는 문항 **5건** 발견:
  `KICE-2022-JUNE-11`, `KICE-2024-JUNE-02`, `KICE-2024-CSAT-07`, `KICE-2025-JUNE-05`,
  `KICE-2026-JUNE-09`. 대부분 I-02(연구 방법)와 I-03(연구 윤리·절차) 경계 오분류다.
  → 구현 단계에서 실제 내용에 맞게 바로잡고 근거를 `kice-analysis.md`에 남긴다.
- **3단계를 3a(데이터) / 3b(렌더러)로 순차 분할한다.** "레이어 분할 금지"는 *서로 다른 모델*이
  한 기능의 레이어를 나눠 갖지 말라는 규칙이므로, **같은 모델(opus 5)이 순차로 이어받는 것**은
  이에 저촉되지 않는다고 판단했다. 근거: 13단원 콘텐츠 전면 재작성 + 78문항 태깅 + 렌더러 +
  CSS + 게이트를 한 세션에 넣으면 컨텍스트가 넘쳐 품질이 떨어진다. 핸드오프는 채팅이 아니라
  커밋과 이 문서로 한다.
- **3b를 병렬이 아니라 순차로 둔 이유**: 3b가 실제 데이터로 곧바로 브라우저 시각 검증을 해야
  하고(§5 마지막 완료 조건), 병렬로 돌리면 검증을 한 번 더 하게 되어 총비용이 커진다.

---

## 11. 3b 완료 후 총괄 중재 (오케스트레이터 판정)

3b 구현 도중 **다른 세션의 디자인 감사 에이전트가 같은 트리에서 병렬로 작업**했다
(`790ab60` 디자인 헌장 신설, `c05ae71` 색 토큰 정리, `bec16b5` 개념 노트 재조판,
`1355494` 중단된 Codex 실행분 보존). 이 사이클의 기획서(plan.md)와 충돌하는 지점을
아래와 같이 판정한다. 판정 기준은 **`docs/DESIGN.md`(디자인 헌장)가 시각 품질의 상위 계약**이고,
plan.md는 이번 사이클의 기능 계약이라는 것이다.

### D-1. 태그 빈출 막대·정답률 게이지 제거 — **감사 결정 유지**
plan.md §4.3은 "태그별 빈출 막대 + 정답률 게이지"를 지시했으나 감사가 이를 숫자 표기로
바꿨다. DESIGN.md §4는 프로그레스 바를 **실제 진행 데이터에만** 허용하고 정적 콘텐츠의
비중 표현에는 금지한다. 태그 빈출도는 후자에 해당하므로 헌장이 옳다. 정답률은 실데이터지만,
개념 화면에는 이미 다이어그램 21개라는 주 시각 요소가 있어 §4의 "장식 프리미티브 2종 이하"에
걸린다. **집계 로직(QUESTIONS 실시간 계산)이 유지되고 표현만 바뀐 것이므로 R5는 충족된다.**
→ plan.md §4.3의 해당 문장은 이 판정으로 대체된다.

### D-2. 폴백 전환을 컨테이너 쿼리로 구현 — **승인**
plan.md §4.3은 "미디어쿼리로"라고 적었으나 3b는 컨테이너 쿼리를 1차 수단으로,
700px 미디어쿼리를 안전망으로 병기했다. 사이드바 때문에 뷰포트 1024px에서도 본문 칼럼이
586px라 뷰포트 기준만으로는 전환 임계가 어긋난다는 근거가 타당하다. 금지 사항이었던
`hidden` 속성은 쓰지 않았으므로 원래 제약의 취지도 지켜졌다.

### D-3. `keyPoints[].icon` — **게이트에서 필수 요구를 해제한다**
감사가 히어로의 아이콘 칩을 제거해 지금은 **렌더되지 않는 필드를 게이트가 강제**하고 있다.
DESIGN.md §4는 "리스트의 기본값은 일반 리스트이며 아이콘 리스트는 순서 자체가 학습 내용일
때만"이라 하므로 아이콘 없는 조판이 헌장에 맞다. 따라서 렌더러가 아니라 **게이트를 고친다**:
`keyPoints[].icon`을 선택 필드로 내리되, 값이 있으면 `ICONS` 키에 존재해야 한다는 검사는 유지한다.
데이터에 남은 미사용 `icon` 값을 지울지는 리뷰어 판정에 맡긴다.

### D-4. 커밋 경계 오염 — **기록만 하고 되돌리지 않는다**
3b의 `3f962bd`가 `git add -A`로 병렬 세션의 미커밋 파일을 함께 담았다. 유실은 없고
작업 트리는 깨끗하다. 이력을 다시 쓰는 것이 더 위험하므로 정정하지 않고 기록만 남긴다.
→ 재발 방지: **병렬 세션이 있을 수 있는 레포에서 `git add -A`를 쓰지 않는다**(회고 후보).

### D-5. 스크린샷 미수행 — **미완료로 명시**
DESIGN.md §6은 시각 품질을 스크린샷으로 확인하라고 요구하고, 불가능하면 명시하라고 한다.
3b는 환경상 스크린샷을 찍지 못했고 대신 실제 브라우저 DOM에서 `getBBox()`로 다이어그램
텍스트 288개의 이탈·겹침을 전수 계측(위반 0)했다. **계측은 시각 확인의 대체가 아니다**(§6).
→ 이 사이클의 시각 확인은 **미완료** 상태이며, 오케스트레이터가 직접 재시도한다.

---

## 12. 6단계(수정 라운드) 판단 기록 — 리뷰 지적 해소

교차 리뷰(`docs/review.md`, blocker 4 / major 10 / nit 2)를 해소하며 내린 판단이다.
사용자 확인을 기다리지 않고 결정한 뒤 여기에 근거를 남긴다.

### D-6. M-2 좌표 불일치 — **상한을 낮추는 쪽으로 통일한다**
리뷰 권고 두 갈래(스키마 상한을 낮추기 / 좌표를 키우기) 중 **낮추기**를 골랐다.
현재 데이터 실측 최댓값이 label 7 / items 14 / center 6 / items 개수 4(scale)라
상한을 label 8 / items 16 / center 8로 내려도 데이터가 그대로 통과하기 때문이다.
같은 숫자를 `diagram.js` 머리 주석과 `scripts/validate.mjs`의
`DIAGRAM_TEXT_LIMITS`·`DIAGRAM_SHAPE_BOUNDS`에 함께 적고 서로를 가리키게 했다.
단, timeline은 상한만으로 부족해 레이아웃도 고쳤다(양 끝 인셋, 위쪽 블록의 커넥터 기점,
높이 고정 해제). `wrapCenter()`의 공백 없는 한글 폴백도 함께 넣었다.

### D-7. `keyPoints[].icon` — **선택 필드로 내리는 대신 데이터에서 삭제한다**
D-3이 "지울지는 리뷰어 판정에 맡긴다"고 남겼다. 삭제를 택했다. 렌더러가 읽지 않는 값이
39개 남아 있으면 다음 사이클에서 "왜 있지"라는 질문이 다시 나오고, 지금 도입한
계약↔렌더러 양방향 대조가 죽은 필드를 상시 금지하므로 예외를 하나 두는 편이 더 비싸다.
아이콘 조판이 다시 필요해지면 렌더러가 읽는 순간 게이트가 계약 추가를 강제한다.

### D-8. B-2 계약 선언의 위치 — **검사 쪽에 두고 렌더러에서 도출해 대조한다**
리뷰는 "렌더러가 읽는 필드에서 도출하거나, 최소한 한 곳에 선언하고 둘이 같은 선언을
참조하라"고 했다. 스키마 선언을 브라우저 런타임(`notebook-data.js`)에 넣으면 스크립트 로드
계약이 늘고 렌더 경로에 검증 코드가 얹힌다. 대신 계약표는 `validate.mjs`에 두고,
`derivedRenderedFields()`가 `app.js`·`diagram.js` 소스에서 실제로 읽는 프로퍼티 경로를
도출해 **양방향**으로 대조한다. 표가 렌더러보다 뒤처지면 게이트가 실패하므로
"한 곳 선언"과 같은 효과를 얻으면서 배포물은 그대로다.

### D-9. 문자열 계약의 범위 — **퀴즈 해설은 넣지 않는다**
B-1을 해소하며 순회를 `data.js`의 `UNITS[].sections`·`VISUAL_GUIDES`·`keywords`와
렌더러 고정 문구까지 넓혔다. `explanation-data.js`의 `GUIDES`는 넣지 않았다.
R1의 "한 문장 60자"는 §1에서 **개념 파트 본문**에 건 계약이고, 오답 해설은 두 문장으로
원인과 처방을 나눠 쓰는 것이 자연스럽기 때문이다. 이 결정을 검사 코드 주석에도 남겼다.

### D-10. M-8·M-9·N-2 — **병렬 세션이 이미 해소했다**
`ec6be4d`(다른 세션)가 sticky 내비게이션의 `blur(20px)` 제거, 감사 문서의 "크롬 레이어 예외"
서술 삭제, 빠른 복습의 `section`+`h3` 의미 구조 복원, 색 토큰 기록 정정을 모두 처리했다.
같은 파일을 다시 고치지 않고 확인만 했다.

### D-11. M-6 시각 확인 — **스냅샷 HTML로 대체하고 그 한계를 명시한다**
브라우저 pane의 뷰포트가 0px라 이미지 캡처가 되지 않는 조건은 6단계에서도 같았다
(총괄도 재현했다). 대신 렌더된 DOM을 독립 실행 HTML로 고정했다:
`docs/snapshots/diagrams.html`(21개 다이어그램 전부)과
`docs/snapshots/concept-sample.html`(III-01 개념 화면 `#app` 전체). CSS 두 개를 인라인해
파일 단독으로 열린다. **스크린샷의 완전한 대체가 아니다** — 사람이 열어서 보는 절차가 남는다.
함께, 계약 상한 데이터로 7종을 전수 렌더해 `getBBox()`로 재측정했다(텍스트 130개,
viewBox 이탈 0, 겹침 0; radial 중심 8자는 두 줄로 접혀 반지름 33.6 < 66). 실데이터
21개·288개 텍스트도 이탈 0 / 겹침 0이다.
→ 이전 라운드 계측이 사실은 무효였다는 점도 기록해 둔다: 이 환경은 `innerWidth`가 0이라
컨테이너 쿼리가 목록 폴백으로 넘어가 `.sm-diagram-canvas`가 `display:none`이 되고,
그 상태의 `getBBox()`는 전부 0을 돌려준다. 즉 "이탈 0 / 겹침 0"이 **공허하게** 참이 된다.
이번에는 폭을 강제하고 `zeroWidth` 카운트를 함께 재어 계측이 실제로 이루어졌음을 확인했다.
